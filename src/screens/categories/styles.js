import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  categoryContainer: {
    marginHorizontal: 14,
    marginVertical: 14,
  },
  listCategory: {
    gap: 14,
    paddingBottom: 20,
  },
});
